# Maps the Upgrade header so WebSocket connections proxy correctly.
# Installed into /etc/nginx/conf.d/ only if no such map already exists.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
