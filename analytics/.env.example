# Copy to /root/fishbowl/analytics/.env on the VPS before first deploy.

# Postgres password for Umami's internal DB. Choose something strong.
UMAMI_DB_PASSWORD=change-me

# Random secret used to sign Umami JWTs.
# Generate with: openssl rand -base64 32
UMAMI_APP_SECRET=change-me
